import React, { useEffect } from 'react';
import Layout from '../layout/layout';

export default function Home() {
    // useEffect(() => {
    //     console.log(window.innerHeight)

    // }, [])
    return <Layout main={undefined}>some</Layout>;
}
